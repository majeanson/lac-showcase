{
  "featureKey": "feat-2026-020",
  "title": "Focus mode: children inherit focused state",
  "status": "frozen",
  "problem": "When a feature is focused, its direct children appeared grey (55% opacity) instead of fully visible, making it harder to read the focused thread.",
  "schemaVersion": 1,
  "analysis": "In focus mode, the app assigns four states to each card: 'focused' (full opacity), 'context' (55%), 'dimmed' (22%), or 'normal'. Previously, both ancestors and direct children got 'context' — both appeared grey. Direct children form the immediate thread and should render at full opacity alongside the focused parent.",
  "implementation": "Split focusContextKeys into two separate memos: focusAncestorKeys (ancestors, stay at 'context' opacity) and focusDescendantKeys (ALL descendants via BFS, promoted to 'focused'). Updated the focusState ternary so node.featureKey === focusKey || focusDescendantKeys.has(node.featureKey) maps to 'focused', with only ancestors falling back to 'context'. Note: the implementation promotes all descendants, not just direct children — the variable was named focusDescendantKeys and uses a queue-based BFS through childrenMap.",
  "tags": [
    "focus-mode",
    "ux",
    "feature-tree"
  ],
  "lineage": {
    "parent": "feat-2026-016",
    "spawnReason": "feat-2026-016 noted ancestors and children shared context opacity — this promotes children to full opacity."
  },
  "decisions": [
    {
      "decision": "Split focusContextKeys into two separate memos — focusAncestorKeys and focusChildKeys — rather than a single combined set",
      "rationale": "Splitting keeps the change minimal and co-located with the code that consumes each set — a single set with a type discriminant would require restructuring the focusState ternary more invasively.",
      "alternativesConsidered": [
        "Single focusContextKeys with a { key, role } shape — requires a Map instead of a Set, adding allocations on every render"
      ],
      "date": "2026-03-21"
    },
    {
      "decision": "Promote children to 'focused' state (same class as the clicked card) rather than a new intermediate state",
      "rationale": "Reusing the existing 'focused' CSS class means no new styling is needed and reinforces that parent + children form a single readable unit.",
      "alternativesConsidered": [
        "New 'child' CSS class at ~80% opacity — adds a fourth visual level, indistinguishable from 'context' at a glance"
      ],
      "date": "2026-03-21"
    },
    {
      "decision": "Only direct children are promoted — grandchildren and deeper remain dimmed",
      "rationale": "Promoting the full subtree would fill the viewport with full-opacity cards, defeating focus mode's purpose — exploring deeper descendants requires a separate focus click.",
      "alternativesConsidered": [
        "Promote all descendants — too much noise, collapses the focused/context distinction for deep trees"
      ],
      "date": "2026-03-21"
    }
  ],
  "knownLimitations": [
    "All descendants render at full 'focused' opacity — very deep trees with many descendants can fill the viewport, reducing focus contrast",
    "Ancestors at 55% opacity feel more faded than descendants at 100%, slightly inverting reading-order intuition",
    "No keyboard shortcut to jump focus to a child feature"
  ],
  "successCriteria": "When a feature is focused, its entire descendant subtree renders at full opacity alongside the focused card, while ancestors appear at 55% and all other features dim to 22%.",
  "domain": "ux",
  "priority": 3,
  "revisions": [
    {
      "date": "2026-03-22",
      "author": "claude",
      "fields_changed": [
        "implementation",
        "successCriteria"
      ],
      "reason": "Code promotes ALL descendants (BFS via focusDescendantKeys), not just direct children as previously documented. Variable is focusDescendantKeys, not focusChildKeys. KnownLimitations and successCriteria updated to match actual behavior."
    }
  ]
}
