{
  "featureKey": "feat-2026-020",
  "title": "Focus mode: children inherit focused state",
  "status": "frozen",
  "problem": "When a feature is focused, its direct children appeared grey (55% opacity) instead of fully visible, making it harder to read the focused thread.",
  "schemaVersion": 1,
  "analysis": "In focus mode, the app assigns four states to each card: 'focused' (full opacity), 'context' (55%), 'dimmed' (22%), or 'normal'. Previously, both ancestors and direct children got 'context' — both appeared grey. Direct children form the immediate thread and should render at full opacity alongside the focused parent.",
  "implementation": "Split focusContextKeys into two separate memos: focusAncestorKeys (ancestors, stay at 'context') and focusChildKeys (direct children, promoted to 'focused'). Updated the focusState ternary so node.featureKey === focusKey || focusChildKeys.has(node.featureKey) maps to 'focused', with only ancestors falling back to 'context'.",
  "tags": ["focus-mode","ux","feature-tree"],
  "lineage": {"parent": "feat-2026-016","spawnReason": "feat-2026-016 noted ancestors and children shared context opacity — this promotes children to full opacity."},
  "decisions": [
    {"decision": "Split focusContextKeys into two separate memos — focusAncestorKeys and focusChildKeys — rather than a single combined set","rationale": "Splitting keeps the change minimal and co-located with the code that consumes each set — a single set with a type discriminant would require restructuring the focusState ternary more invasively.","alternativesConsidered": ["Single focusContextKeys with a { key, role } shape — requires a Map instead of a Set, adding allocations on every render"],"date": "2026-03-21"},
    {"decision": "Promote children to 'focused' state (same class as the clicked card) rather than a new intermediate state","rationale": "Reusing the existing 'focused' CSS class means no new styling is needed and reinforces that parent + children form a single readable unit.","alternativesConsidered": ["New 'child' CSS class at ~80% opacity — adds a fourth visual level, indistinguishable from 'context' at a glance"],"date": "2026-03-21"},
    {"decision": "Only direct children are promoted — grandchildren and deeper remain dimmed","rationale": "Promoting the full subtree would fill the viewport with full-opacity cards, defeating focus mode's purpose — exploring deeper descendants requires a separate focus click.","alternativesConsidered": ["Promote all descendants — too much noise, collapses the focused/context distinction for deep trees"],"date": "2026-03-21"}
  ],
  "knownLimitations": [
    "Grandchildren at depth 2+ still render dimmed — deep threads require multiple focus clicks",
    "A focused feature with 8+ children fills the viewport, reducing focus contrast",
    "Ancestors at 55% opacity feel more faded than children at 100%, slightly inverting reading-order intuition"
  ],
  "successCriteria": "When a feature is focused, its direct children render at full opacity alongside the focused card, while ancestors appear at 55% and all other features dim to 22%.",
  "domain": "ux",
  "priority": 3
}
